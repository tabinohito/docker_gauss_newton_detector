# Docker_gauss_newton_detector
## Build Docker
- Normal case
```
sudo ./build.sh
```

- To avoid having to use sudo when you use the docker command
```
./build.sh
```

## Run Docker
- Normal case
```
sudo ./run.sh
```

- To avoid having to use sudo when you use the docker command
```
./run.sh
```
